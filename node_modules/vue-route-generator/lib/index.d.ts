export interface GenerateConfig {
    pages: string;
    importPrefix?: string;
    dynamicImport?: boolean;
    nested?: boolean;
}
export declare function generateRoutes({ pages, importPrefix, dynamicImport, nested }: GenerateConfig): string;
